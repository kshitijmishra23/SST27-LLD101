package com.example.imageviewer;

import java.util.ArrayList;
import java.util.List;

public class Demo {
    public static void main(String[] args) {
        // TODO 1: Create a List<Image> of 5 ImageProxy objects with file names like
        //         "landscape.jpg", "portrait.jpg", "skyline.jpg", "waterfall.jpg", "sunset.jpg".
        //         Do NOT use HighResImage directly — the proxy must handle loading.

        // TODO 2: Print each image's file name (getFileName) — notice: no disk loading yet.

        // TODO 3: Call display() only on images at index 0 and 2.
        //         Confirm in the output that only those two images were loaded from disk.
        //         Call display() on index 0 a second time and verify it does NOT reload from disk.

        System.out.println("=== Image Gallery ===");
        System.out.println();
        System.out.println("Listing all images (no loading expected):");

        // Your code here
    }
}
